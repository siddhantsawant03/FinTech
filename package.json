{
  "name": "wealth-allocator-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "postinstall": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"yellow,cyan\" \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
